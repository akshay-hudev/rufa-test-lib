{
  "name": "rufa-test-lib",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "build": "npm run clean && tsc",
    "test": "npm run build && node --test test/*.test.js"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
